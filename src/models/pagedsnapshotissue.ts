/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import { SnapshotIssue, SnapshotIssue$inboundSchema } from "./snapshotissue.js";

export type PagedSnapshotIssue = {
  /**
   * Current page number
   */
  currentPage: number;
  /**
   * Whether there is a next page
   */
  hasNext: boolean;
  /**
   * Whether there is a previous page
   */
  hasPrevious: boolean;
  items: Array<SnapshotIssue>;
  /**
   * Total number of items
   */
  totalCount: number;
  /**
   * Total number of pages
   */
  totalPages: number;
};

/** @internal */
export const PagedSnapshotIssue$inboundSchema: z.ZodType<
  PagedSnapshotIssue,
  z.ZodTypeDef,
  unknown
> = z.object({
  current_page: z.number().int(),
  has_next: z.boolean(),
  has_previous: z.boolean(),
  items: z.array(SnapshotIssue$inboundSchema),
  total_count: z.number().int(),
  total_pages: z.number().int(),
}).transform((v) => {
  return remap$(v, {
    "current_page": "currentPage",
    "has_next": "hasNext",
    "has_previous": "hasPrevious",
    "total_count": "totalCount",
    "total_pages": "totalPages",
  });
});

export function pagedSnapshotIssueFromJSON(
  jsonString: string,
): SafeParseResult<PagedSnapshotIssue, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PagedSnapshotIssue$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PagedSnapshotIssue' from JSON`,
  );
}
